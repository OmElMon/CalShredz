// Import the custom `useToast` hook and `toast` utility from the toast hook file
import { useToast, toast } from "@/hooks/UseToast";

// Re-export them for easier and centralized access across the app
export { useToast, toast };